import type { XPCDictionary } from '../types.js';
export declare const XPC_TYPES: {
    [key: string]: number;
};
export interface XPCMessage {
    flags: number;
    id?: bigint | number;
    body?: XPCDictionary | null;
    size?: number;
}
export interface DecodedXpcMessage {
    message: XPCMessage;
    /** Bytes consumed from `buffer` (may be less than `buffer.length`). */
    bytesConsumed: number;
}
/**
 * Encodes a message object into an XPC Buffer.
 * A message is an object like:
 *   { flags: Number, id: bigint, body: Object|null }
 */
export declare function encodeMessage(message: XPCMessage): Buffer;
/**
 * Returns the total byte length of one length-prefixed XPC wrapper in `buffer`.
 * @throws if the header is missing, invalid, or the buffer is shorter than the message
 */
export declare function getXpcMessageByteLength(buffer: Buffer): number;
/**
 * Decodes an XPC Buffer into a message object.
 * (Keep this function if you plan to handle incoming messages.)
 */
export declare function decodeMessage(buffer: Buffer): DecodedXpcMessage;
//# sourceMappingURL=xpc-protocol.d.ts.map