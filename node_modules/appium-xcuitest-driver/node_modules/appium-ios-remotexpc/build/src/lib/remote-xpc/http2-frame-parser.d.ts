export interface ParsedDataFrame {
    readonly streamId: number;
    readonly data: Buffer;
    readonly bodyLen: number;
}
export type ParsedFrame = {
    readonly type: 'data';
    readonly frame: ParsedDataFrame;
} | {
    readonly type: 'other';
};
/**
 * Incrementally parse HTTP/2 frames from a byte stream (RFC 7540).
 */
export declare class Http2FrameParser {
    private buffer;
    append(chunk: Buffer): ParsedFrame[];
}
/**
 * Emit WINDOW_UPDATE frames for even-numbered streams, matching `remoted` behavior.
 */
export declare function buildWindowUpdateFrames(streamId: number, increment: number): Buffer[];
//# sourceMappingURL=http2-frame-parser.d.ts.map