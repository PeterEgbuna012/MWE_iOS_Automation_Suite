import { EventEmitter } from 'node:events';
export interface RemoteXpcFramedTransportConnectOptions {
    timeoutMs: number;
    handshakeDelayMs?: number;
}
/**
 * Shared RemoteXPC transport: owns TCP socket lifecycle, HTTP/2/XPC handshake,
 * DATA frame parsing, window updates, and XPC message reassembly.
 */
export declare class RemoteXpcFramedTransport extends EventEmitter {
    private readonly address;
    private socket;
    private frameParser;
    private pendingXpcData;
    private connected;
    private closing;
    constructor(address: [string, number]);
    get isConnected(): boolean;
    connect(options: RemoteXpcFramedTransportConnectOptions): Promise<void>;
    sendDataFrame(payload: Buffer, streamId?: number): void;
    close(): Promise<void>;
    private waitForSocketConnect;
    private registerSocketHandlers;
    private handleData;
    private ingestXpcData;
    private shutdownSocket;
    private resetParsers;
}
//# sourceMappingURL=remote-xpc-framed-transport.d.ts.map