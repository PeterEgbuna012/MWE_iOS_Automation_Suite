import { type Socket } from 'node:net';
/**
 * Default upstream connector backed by usbmux.
 */
export declare const connectViaUsbmux: (udid: string, devicePort: number, connectTimeoutMs?: number) => Promise<Socket>;
/**
 * Resolve the current tunnel host from the registry, then connect to `devicePort`.
 */
export declare const connectViaTunnel: (udid: string, devicePort: number, connectTimeoutMs?: number) => Promise<Socket>;
//# sourceMappingURL=connectors.d.ts.map