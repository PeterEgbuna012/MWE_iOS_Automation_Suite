import { EventEmitter } from 'node:events';
import type { DevicePortForwarderEvents, DevicePortForwarderOptions } from './types.js';
/**
 * A lifecycle-managed local forwarder that proxies local TCP clients to a device port.
 * A fresh upstream socket is created per local client connection.
 */
export declare class DevicePortForwarder extends EventEmitter {
    private server?;
    private readonly localPort;
    private readonly devicePort;
    private readonly host;
    private readonly primaryConnector;
    private readonly fallbackConnector?;
    private readonly activeSockets;
    constructor(localPort: number, devicePort: number, options: DevicePortForwarderOptions);
    /**
     * Starts the local forwarding server.
     */
    start(): Promise<void>;
    /**
     * Stops the forwarding server and closes active sockets.
     */
    stop(): Promise<void>;
    on<K extends keyof DevicePortForwarderEvents>(eventName: K, listener: DevicePortForwarderEvents[K]): this;
    once<K extends keyof DevicePortForwarderEvents>(eventName: K, listener: DevicePortForwarderEvents[K]): this;
    off<K extends keyof DevicePortForwarderEvents>(eventName: K, listener: DevicePortForwarderEvents[K]): this;
    private handleDownstreamConnection;
    private openUpstreamSocket;
}
//# sourceMappingURL=device-port-forwarder.d.ts.map