import type { DiscoveredDevice, DiscoveryOptions, IDeviceDiscoveryBackend } from './types.js';
/**
 * Common scaffolding for discovery backends.
 *
 * Centralizes the in-flight deduplication so concurrent `discoverDevices`
 * calls share a single underlying browse, and provides default options.
 * Concrete backends only implement `runDiscovery`.
 */
export declare abstract class BaseDiscoveryBackend implements IDeviceDiscoveryBackend {
    protected readonly options: DiscoveryOptions;
    private inFlightDiscovery?;
    protected constructor(options?: DiscoveryOptions);
    discoverDevices(timeoutMs: number): Promise<DiscoveredDevice[]>;
    protected get serviceType(): string;
    protected get domain(): string;
    protected abstract runDiscovery(timeoutMs: number): Promise<DiscoveredDevice[]>;
}
//# sourceMappingURL=base-discovery-backend.d.ts.map