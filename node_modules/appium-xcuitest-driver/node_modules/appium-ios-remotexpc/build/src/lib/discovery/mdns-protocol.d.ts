/**
 * Minimal DNS/mDNS packet helpers for raw service browsing.
 *
 * No third-party mDNS library and no RFC 6335 service-name length checks, so
 * Apple's long DNS-SD types work.
 */
export declare const MDNS_PORT = 5353;
export declare const MDNS_MCAST_V4 = "224.0.0.251";
export declare const MDNS_MCAST_V6 = "ff02::fb";
export declare const QTYPE_A = 1;
export declare const QTYPE_PTR = 12;
export declare const QTYPE_TXT = 16;
export declare const QTYPE_AAAA = 28;
export declare const QTYPE_SRV = 33;
export interface ParsedResourceRecord {
    name: string;
    type: number;
    class: number;
    ttl: number;
    ptrdname?: string;
    priority?: number;
    weight?: number;
    port?: number;
    target?: string;
    txt?: Record<string, string>;
    address?: string;
    raw?: Buffer;
}
/** Encode a dotted DNS name as a length-prefixed wire-format name. */
export declare function encodeName(name: string): Buffer;
/** Decode a wire-format DNS name starting at `offset`. */
export declare function decodeName(data: Buffer, offset: number): {
    name: string;
    offset: number;
};
/** Build a DNS query packet with a single question. */
export declare function buildQuery(name: string, qtype: number, unicast?: boolean): Buffer;
/** Parse answer/authority/additional records from an mDNS response packet. */
export declare function parseMdnsMessage(data: Buffer): ParsedResourceRecord[];
/**
 * Decode DNS-SD decimal escape sequences in instance names (e.g. `\032` → space).
 *
 * Per RFC 6763, `\DDD` uses three decimal digits for the byte value, not octal.
 */
export declare function decodeDnsSdInstanceName(name: string): string;
/** Build a trailing-dot FQDN from a DNS-SD type and domain (e.g. `local`). */
export declare function buildServiceTypeFqdn(serviceType: string, domain: string): string;
//# sourceMappingURL=mdns-protocol.d.ts.map