/**
 * Strip a single trailing dot from an FQDN-style value (e.g. `local.` → `local`).
 */
export declare function stripTrailingDot(value: string): string;
/**
 * Ensure hostnames are returned in fqdn form with trailing dot.
 */
export declare function normalizeHostname(host?: string): string | undefined;
/**
 * Resolve a preferred IPv4 address from service data or DNS lookup.
 */
export declare function resolveIpAddress(host?: string, addresses?: string[]): Promise<string | undefined>;
//# sourceMappingURL=discovery-utils.d.ts.map