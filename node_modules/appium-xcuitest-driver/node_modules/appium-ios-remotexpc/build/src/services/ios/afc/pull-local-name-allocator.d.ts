export type CaseSensitiveDirectoryCheck = (dir: string) => Promise<boolean>;
/**
 * Resolves sanitized local path segment names during a single AFC pull.
 * Adds a UUID-based suffix when the sanitized name is already taken in this pull, or
 * when `overwrite` is false and a file already exists at that path on disk.
 */
export declare class PullLocalNameAllocator {
    private readonly pathExists;
    private readonly overwrite;
    private readonly checkCaseSensitive;
    private readonly usedByDir;
    private readonly caseSensitiveByDir;
    constructor(pathExists: (localPath: string) => Promise<boolean>, overwrite: boolean, checkCaseSensitive?: CaseSensitiveDirectoryCheck);
    allocate(parentDir: string, remoteSegment: string): Promise<string>;
    private hasClash;
    private isUsedInPull;
    private namesCollide;
    private isCaseSensitiveForDir;
    private register;
}
//# sourceMappingURL=pull-local-name-allocator.d.ts.map