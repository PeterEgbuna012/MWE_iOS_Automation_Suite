import { type ZipConduitProgressUpdate } from './plists.js';
export type ZipConduitProgressCallback = (update: ZipConduitProgressUpdate) => void;
export interface ZipConduitInstallOptions {
    progress?: ZipConduitProgressCallback;
    timeoutMs?: number;
    /** Stop after payload upload; skip waiting for install progress plists. */
    streamOnly?: boolean;
}
export interface ZipConduitStreamStats {
    streamMs: number;
    payloadBytes: number;
    entryCount: number;
}
/**
 * Streaming zip_conduit client for fast IPA installation over RSD.
 */
export declare class ZipConduitService {
    private readonly udid;
    static readonly RSD_SERVICE_NAME = "com.apple.streaming_zip_conduit.shim.remote";
    private socket;
    constructor(udid: string);
    /**
     * Connect to the zip_conduit service and complete the RSD handshake.
     */
    connect(): Promise<void>;
    /**
     * Install an IPA or app directory using streaming zip_conduit.
     */
    install(appPath: string, options?: ZipConduitInstallOptions): Promise<ZipConduitStreamStats | void>;
    /**
     * Close the underlying socket.
     */
    close(): void;
    private sendIpaFile;
    private waitForInstallation;
}
export default ZipConduitService;
//# sourceMappingURL=index.d.ts.map