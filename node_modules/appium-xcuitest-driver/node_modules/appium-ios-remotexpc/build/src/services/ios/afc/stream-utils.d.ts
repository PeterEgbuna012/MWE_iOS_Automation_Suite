import { Readable, Writable } from 'node:stream';
import { AfcError, AfcOpcode } from './enums.js';
export type AfcSendAndWait = (op: AfcOpcode, headerPayload?: Buffer, content?: Buffer) => Promise<{
    status: AfcError;
    data: Buffer;
}>;
export type AfcFileWriteAndWait = (handlePayload: Buffer, content: Buffer) => Promise<{
    status: AfcError;
    data: Buffer;
}>;
/**
 * Create a readable stream that pulls file chunks over AFC READ requests.
 */
export declare function createAfcReadStream(handle: bigint, size: bigint, sendAndWait: AfcSendAndWait): Readable;
/**
 * Create a writable stream that pushes data over AFC WRITE requests.
 */
export declare function createAfcWriteStream(handle: bigint, writeChunk: AfcFileWriteAndWait, chunkSize?: number): Writable;
//# sourceMappingURL=stream-utils.d.ts.map