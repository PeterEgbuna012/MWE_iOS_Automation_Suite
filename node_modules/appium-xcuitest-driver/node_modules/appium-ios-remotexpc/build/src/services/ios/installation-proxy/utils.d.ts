import type { InstallAction, InstallOperationResult } from './types.js';
/**
 * Constants for install/upgrade operation messages
 */
export declare const INSTALL_MESSAGES: {
    readonly FRESH_INSTALL: "App was not previously installed";
    readonly VERSION_UNKNOWN: "Current version could not be determined";
    readonly DOWNGRADE_NOT_SUPPORTED: "Downgrades are not supported by iOS installation_proxy";
    readonly SAME_VERSION_NOT_SUPPORTED: "Reinstalling the same version is not supported by iOS installation_proxy";
};
/**
 * Compare two version strings
 * @param version1 First version string (e.g., '1.2.3')
 * @param version2 Second version string (e.g., '1.2.4')
 * @returns -1 if version1 < version2, 0 if equal, 1 if version1 > version2
 */
export declare function compareVersions(version1: string, version2: string): number;
/**
 * Create standardized result object
 */
export declare function createResult(action: InstallAction, reason: string, targetVersion: string, currentVersion?: string): InstallOperationResult;
/**
 * Check if installation should proceed based on version comparison
 * @returns Result object with action and reason
 */
export declare function shouldInstall(isInstalled: boolean, targetVersion: string): InstallOperationResult | null;
/**
 * Check if upgrade should proceed based on version comparison
 * @returns Result object with action and reason, or null to proceed
 */
export declare function shouldUpgrade(isInstalled: boolean, currentVersion: string | undefined, targetVersion: string): InstallOperationResult | null;
//# sourceMappingURL=utils.d.ts.map