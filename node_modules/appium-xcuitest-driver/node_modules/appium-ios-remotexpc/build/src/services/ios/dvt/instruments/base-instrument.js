/**
 * Base class for DVT instrument services.
 *
 * Subclasses must define a static `IDENTIFIER` property.
 */
export class BaseInstrument {
    dvt;
    static IDENTIFIER;
    channel = null;
    constructor(dvt) {
        this.dvt = dvt;
    }
    get identifier() {
        return this.constructor.IDENTIFIER;
    }
    /**
     * Initialize the instrument channel.
     */
    async initialize() {
        if (!this.channel) {
            this.channel = await this.dvt.makeChannel(this.identifier);
        }
    }
    requireChannel() {
        if (!this.channel) {
            throw new Error(`${this.constructor.name} channel is not initialized. Call initialize() first.`);
        }
        return this.channel;
    }
}
//# sourceMappingURL=base-instrument.js.map